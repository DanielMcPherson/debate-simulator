node_modules/
dist/
*.log
.DS_Store
.vite/

# Secrets — API keys for the offline art-generation script (NEVER commit)
.env
*.env

# Concept-art references (ChatGPT mockups) — kept locally, too large to version
concept_art/

# Emacs backup / auto-save / lock files
*~
\#*\#
.\#*
CARD_SCORES.md
CARD_STRENGTH.md
UPGRADE_WORKLIST.md
UPGRADE_REVIEW.md
OPPONENT_VOICES.md
CARD_CURATION.md

# Commercialization strategy doc — private (repo is public via GitHub Pages)
RELEASE_ROADMAP.md

# Stitched TTS preview statements (gen-tts.mjs --preview) — listen-and-delete, never shipped
voice-preview/

# Raw TTS wavs (gen-tts.mjs synth cache) — post-processing knobs re-run from these for free
voice-raw/
